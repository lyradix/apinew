{% extends 'base.html.twig' %}

{% block title %}Hello IndexController!{% endblock %}

{% block body %}
<a href="{{ path('app_adminConcerts') }}" class="btn btn-success"> 
    <img class="logoXtra" src="{{ asset('images/preview.png') }}" alt="logo">
</a>
<h1>Choisir le lieu</h1>

<ul class="wrapbox">
{% for poi in pois %}
    <li>
        <a href="{{ path('update_poi', {'id': poi.id}) }}">
            <div class="btnConcert">
                <p class="text">{{ poi.properties.popup }}</p>
            </div>
        </a>
    </li>
{% else %}
    <li>Aucun lieu trouvé.</li>
{% endfor %}
</ul>
<div>
    <button class="box" type="button" onclick="window.history.back()">Retour</button>
</div>


{% endblock %}